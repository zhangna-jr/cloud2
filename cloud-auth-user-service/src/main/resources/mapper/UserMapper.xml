<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zn.cloud.mapper.UserMapper">

    <insert id="create" parameterType="com.zn.cloud.entity.User" >
        insert into test_cloud.user(username,password)
        values
        (#{user.username},#{user.password});

    </insert>

    <resultMap id="BASE_DATE" type="com.zn.cloud.entity.User">
        <id column="id" property="id" jdbcType="BIGINT"></id>
        <id column="username" property="username" jdbcType="VARCHAR"></id>
        <id column="password" property="password" jdbcType="VARCHAR"></id>
    </resultMap>

    <!--<select id="selectById" parameterType="java.lang.Long" resultMap="BASE_DATE">
            select * from test_cloud.payment  where id=#{id}
    </select>-->
</mapper>
